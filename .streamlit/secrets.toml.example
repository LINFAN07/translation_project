# Copy to .streamlit/secrets.toml on Streamlit Cloud (do not commit secrets.toml)
# https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/secrets-management

# GEMINI_WORKER_URL = "https://translation-gemini-proxy.your-account.workers.dev"
# GEMINI_WORKER_AUTH = "same-as-WORKER_AUTH_SECRET-in-Cloudflare"

# Optional: only if NOT using the Worker
# GEMINI_API_KEY = "..."
